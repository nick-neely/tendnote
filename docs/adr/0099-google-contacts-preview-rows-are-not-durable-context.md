# Google Contacts Preview Rows Are Not Durable Context

Phase 2E should keep unconfirmed Google Contacts preview rows ephemeral or short-lived import-session state rather than storing them as normal source records, memories, people, or contact methods. Only user-confirmed import outcomes should write durable Tendnote data, with minimized provenance where useful, so Google Contacts import remains a preview-and-confirm flow instead of a shadow address book or automatic relationship-context source.
