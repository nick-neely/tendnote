# Explicit Memory Capture Keeps Source Record

Phase 1A explicit memory capture should create both a source record and an approved memory that points back to it. Requests such as "remember", "save", "note", or "keep track of" can approve the memory immediately, but they should not skip the evidence layer.

This keeps provenance, audit behavior, retrieval grounding, and future source inspection consistent across manual memories and extracted suggested memories.
