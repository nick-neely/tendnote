# Restricted Sensitivity Not Private

Phase 1A should use `restricted` as the highest sensitivity value instead of `private`. `private` should describe visibility scope, while sensitivity should describe how delicate or constrained the content is.

The preferred values are `normal`, `sensitive`, and `restricted`, paired independently with scope values such as `private`, `shared`, and `household`.
