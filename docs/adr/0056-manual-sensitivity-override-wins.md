# Manual Sensitivity Override Wins

Phase 1A should allow sensitivity to be defaulted by simple rules or future model classification, but user correction must take priority. Memories and source records should expose sensitivity during review or edit flows so users can mark content as normal, sensitive, or private.

Sensitivity affects briefs, drafts, embeddings, retention, and future sharing, so automatic classification should assist the user rather than silently control the policy.
