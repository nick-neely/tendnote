# Contact Methods Store Display and Normalized Values

Phase 2E should add separate display and normalized representations for contact methods rather than overloading the existing value as both UI/sendable text and a matching key. Email display values can remain suitable for Gmail draft recipients while normalized values support owner-wide dedupe, and phone numbers need distinct display and canonical matching forms because provider formatting is not stable enough for identity matching.
