# Google Contacts Import Does Not Silently Overwrite Profile Data

Phase 2E should let confirmed Google Contacts import candidates add missing Tendnote person fields, birthdays, and contact methods, but provider values that conflict with existing user-entered Tendnote data should require explicit resolution. Tendnote remains the relationship notebook source of truth, so a bulk import confirmation must not silently replace existing profile data with Google Contacts values.
