# Notes Are Source Records

Phase 1A should not add a separate `notes` concept or table. Manual notes, interaction summaries, import previews, calendar summaries, email summaries, and other retained context should all be represented as source records with a `source_type`.

If a note is worth retaining, it is evidence; if it becomes durable relationship context, Tendnote should create or promote a memory from that source record.
