# Household Workspace Is the Phase 4 Permission Anchor

Phase 4 should introduce a real Household Workspace as the shared permission anchor rather than treating shared context as a flag on one owner's account. The workspace may support multiple invited members with roles and visibility controls, but it should stay lightweight: no multi-workspace switching, organization/team product, CRM account model, or broad admin console until a later need proves that complexity.
