# Display Name Only Person Creation

Phase 1A should allow person creation with only a display name. Birthday, relationship type, profile blurb, and contact methods should be optional so quick capture and person resolution stay lightweight.

Missing contact information can be surfaced later as a gentle profile-completion prompt, but it should not block relationship memory capture.
