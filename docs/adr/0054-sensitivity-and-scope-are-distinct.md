# Sensitivity And Scope Are Distinct

Phase 1A should keep both `sensitivity` and `scope` because they answer different questions. `sensitivity` describes how delicate the content is; `scope` describes who is allowed to see or use it.

All Phase 1A records should default to private scope before shared household behavior exists. Keeping these fields distinct avoids a future migration where content sensitivity and visibility were accidentally conflated.
