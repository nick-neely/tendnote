# Professional Relationship Type Not Client

Phase 1A should use `professional` instead of `client` as a relationship type. Consultant relationships are valid Tendnote relationships, but `client` pulls the product toward CRM language and pipeline expectations.

`professional` should cover work relationships broadly, while `colleague` and `networking` can remain for more specific cases.
