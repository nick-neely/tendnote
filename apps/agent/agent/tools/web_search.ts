import { disableTool } from "eve/tools";

/**
 * Tendnote answers are grounded in the user's own records with canonical
 * citations and honest limitations. A web result has no citation, no trust level,
 * and no owner, so it can only dilute that contract.
 */
export default disableTool();
